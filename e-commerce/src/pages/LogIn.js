import Header from "../components/layout/Header";

function LogIn({ data }) {
  return (
    <div className="LogIn">
      <Header data={data} />
      <div></div>
    </div>
  );
}

export default LogIn;
